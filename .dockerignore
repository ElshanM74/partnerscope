# Keep build context small — speeds up builds and prevents secrets leaking in.

**/node_modules
**/dist
**/.astro
**/.turbo
**/coverage
# tsc incremental stamps — MUST be excluded or the Docker build skips compilation
# (tsc sees the stamp, thinks artefacts already exist, never writes new dist/).
**/*.tsbuildinfo

# Local env files — secrets live on the server, not in images.
.env
.env.*
!.env.example
apps/*/.env
apps/*/.env.*

# VCS + editor
.git
.gitignore
.github
.vscode
.idea
.DS_Store
*.swp

# CI artefacts
**/*.log
**/npm-debug.log*
**/pnpm-debug.log*

# Tests + docs — not needed in the runtime image
docs
ops
**/*.test.ts
**/*.spec.ts
